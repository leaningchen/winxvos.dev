#include <types.h>
#include <param.h>
#include <cpu.h>

/* CPU 数组 — 每个 CPU 核心对应一个 */
struct cpu cpus[NCPU];

/*---------------------------------------------------------------------------
 * cpuid — 获取当前 CPU 编号
 * TODO: 优化为使用 GS 段寄存器存储 CPU 编号
 * 当前简化实现: 返回 0 (仅 BSP 在运行)
 *---------------------------------------------------------------------------*/
int cpuid(void)
{
    return 0;
}

/*---------------------------------------------------------------------------
 * mycpu — 获取当前 CPU 结构体指针
 *---------------------------------------------------------------------------*/
struct cpu *mycpu(void)
{
    return &cpus[cpuid()];
}